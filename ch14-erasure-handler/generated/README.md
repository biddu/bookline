# ch14-erasure-handler — generated outputs

All runs: Claude (Cowork), model claude-fable-5, 11 August 2026, clean context with an
explicit no-tool instruction, **zero tool calls confirmed in every run**. No edits.

The run named as the Exhibit in RESULTS.md is the one printed in the book. The second run
of each pair is the counter-run, read alongside it wherever the two disagree, because a
disagreement between two runs of one prompt is itself a finding.
